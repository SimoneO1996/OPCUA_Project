import { SessionContext, IEventData, AddressSpace } from "../../source";
import { ContentFilter } from "node-opcua-types";
export declare function checkWhereClause(addressSpace: AddressSpace, sessionContext: SessionContext, whereClause: ContentFilter, eventData: IEventData): boolean;
